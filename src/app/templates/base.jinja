<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block tab_title %}{% endblock %}</title>

    {% block styles%}
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/static/css/style.css">
    {% endblock %}

    {% block scripts %}{% endblock %}
</head>
<body>
    <div class="container">
        {% block header %}
        <div class="header">
            <h1><i class="fas fa-search"></i>{% block page_title %}{% endblock %}</h1>
            <div class="theme-switch-wrapper" style="display: none;">
                <span class="theme-text">Dark Mode</span>
                <label class="theme-switch">
                    <input type="checkbox" id="theme-toggle">
                    <span class="slider">
                        <span class="moon"><i class="fas fa-moon"></i></span>
                        <span class="sun"><i class="fas fa-sun"></i></span>
                    </span>
                </label>
            </div>
        </div>
        {% endblock %}
        
        {% block main_content %}
        {% endblock %}

        {% block footer %}
        {% endblock %}
    </div>
</body>
</html>